
## Example simple api in hapi framework (Nodejs)

## Getting started

* Requirements:
    * [Node.js](http://nodejs.org): `brew install node` en OS X

* Install:
    * `npm install`

* Run:
    * `npm start`
